import React, { FunctionComponent } from 'react'


interface Props {
    id: number;
    name: string;
    image: string;
    price: number;
    category: string;
    // those can be changed depending on the real data
}

const ProductCard: FunctionComponent = () => {
  return (
    <div>
      
    </div>
  )
}

export default ProductCard